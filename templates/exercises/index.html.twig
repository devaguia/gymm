{% extends 'admin.html.twig' %}

{% block title %}Exercises{% endblock %}

{% block body %}
    <div class="flex flex-col md:flex-row w-full">
        <div class="w-full md:w-1/2 flex items-end mt-4">
            <h2 class="font-bold text-2xl">Your exercises</h2>
        </div>
        <div class="w-full md:w-1/2 flex flex-row justify-end mt-2 md:mt-5">
            <a class="gymm-btn-primary md:w-48" href="{{ path('app_add_exercise') }}">+ Add new exercises</a>
        </div>
    </div>
    <hr class="w-full my-4">
    <div>
        {% for exercise in exerciseList %}
            <div class="mt-4">
                <div class="shadow-md rounded-lg w-full py-3 px-4 border border-gray-100 flex flex-row text-sm">
                    <div class="flex flex-row w-1/2 selection:bg-transparent">
                        <span class="w-full md:w-full">{{ exercise.name }}</span>
                    </div>
                    <div class="w-1/2 flex justify-end">
                        <div class="px-4 flex items-center">
                            <a href="{{ path('app_edit_exercise', {exercise: exercise.id}) }}" class="transition-all duration-200 filter hover:opacity-50">
                                <img class="w-[18px]" src="{{ asset('images/icons/pen-to-square-solid.svg') }}" alt="">
                            </a>
                        </div>
                        <form class="flex items-center" method="post" action="{{ path('app_delete_exercise', {id: exercise.id}) }}">
                            <input type="hidden" value="DELETE" name="_method">
                            <button class="w-[16px] transition-all duration-200 filter hover:opacity-50">
                                <img src="{{ asset('images/icons/trash-can-regular.svg') }}" alt="">
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
